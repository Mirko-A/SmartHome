#include "hal.h"

//#include <wiringPi.h>
//#include <softPwm.h>

HAL::HAL()
{
    //m_InitOK = (wiringPiSetup() >= 0);
    // TODO: change first arg, this will be used for the motor
    //m_InitOK = (softPwmCreate(28, 0, 100)!=0);
};

